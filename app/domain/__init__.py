"""Pure business logic: signal detection and risk scoring.

Nothing in this package performs I/O or talks to the network, so the rules that
matter most for audit and correctness are trivially unit-testable in isolation.
"""
